---
import Card from "../primitives/Card.astro";
import TagChip from "./TagChip.astro";
import StatusBadge from "./StatusBadge.astro";
import ResponsiveImage from "./ResponsiveImage.astro";

interface Props {
  title: string;
  slug: string;
  summary: string;
  tags: string[];
  status: "ongoing" | "completed";
  featured: boolean;
  primaryImage?: { src: string; alt?: string };
}

const { title, slug, summary, tags, status, featured, primaryImage } =
  Astro.props;

const displayTags = tags.slice(0, 3);
const extraCount = tags.length - displayTags.length;
---

<Card href={`/projects/${slug}`} class={featured ? "border-(--color-accent)/30" : undefined} data-hover="lift">
  {
    primaryImage && (
      <div class="aspect-video overflow-hidden rounded mb-3">
        <ResponsiveImage
          src={primaryImage.src}
          alt={primaryImage.alt ?? title}
          variant="card"
          class="w-full h-full object-cover"
        />
      </div>
    )
  }
  <div class="flex items-center justify-between gap-2 mb-2">
    <h3 class="font-semibold truncate">{title}</h3>
    <StatusBadge status={status} />
  </div>
  <p class="text-sm text-(--color-text-muted) mb-3 line-clamp-2">{summary}</p>
  <div class="flex gap-1.5 flex-wrap">
    {displayTags.map((tag) => <TagChip tag={tag} interactive={false} />)}
    {extraCount > 0 && (
      <span class="text-xs text-(--color-text-muted) self-center">+{extraCount}</span>
    )}
  </div>
</Card>
